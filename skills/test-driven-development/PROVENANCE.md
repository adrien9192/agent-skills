# Provenance

Original synthesis derived from two MIT-licensed public references:

- `obra/superpowers`, entry `test-driven-development`, observed at v5.0.5;
- `mattpocock/skills`, entry `tdd`, observed on 2026-08-23.

This adaptation keeps the strict observed RED-GREEN loop, adds public seams,
independent oracles, vertical slices, boundary-only mocking, and a non-destructive
legacy-code path. Wording and runtime-neutral contract are maintained here.
